// TODO: Populated by the autonomous_progress feature subagent.
// Each kind component receives `payload: KoraArtifact['payload']` and renders
// the kind-specific preview body inside the shared <ArtifactCard> shell.
export {};
